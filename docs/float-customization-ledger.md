# Effy 的 float 二改：成果与后续记录

更新：2026-09-12。此文档是长对话的接续索引，后续每轮有实质变更时应更新；不是所有功能已完成验收的承诺。

## 当前预览入口（用户已确认，以此为准）

### 2026-09-12 手机粘贴后底部添加按钮修复（未提交）

- 用户截图显示已粘贴、未先点“添加 URL”时底部仍禁用。底部动作现在直接解析 textarea 当前内容并与已有待添加项合并保存；实时显示识别数量，手机 input 事件同步状态，提交再次读取当前 DOM 值。预览改为可选的“预览并编辑”，按钮不换行。
- 直接保存失败时条目留在编辑列表供重试；无可用链接时可点击得到说明，不再只有灰按钮。兼容截图的“描述--”换行 URL，并清理描述结尾分隔符。
- check-sticker-import-ui.cjs 用手机尺寸／触摸的隔离 React 弹窗验证不经预览直接添加 500 条、无效文本提示和失败重试；解析／存储回归通过。不是 iOS 真机自动化，手机最终验收仍用 3003。

### 2026-09-12 表情包批量导入优化（未提交）

- 新增 lib/sticker-import-parser.ts：从描述＋HTTP(S) 链接提取多组条目，兼容跨行、同一行多条、Markdown、表格／CSV、JSON 数组／对象／逐行 JSON；保留查询参数及平衡括号，过滤无效 URL，重复链接计数，重名自动编号。
- sticker-manager 批量弹窗接入解析与识别数量提示；有无效链接时保留原文供修正。可用项独立导入，空名／重名等坏项不再挡住全部；文件失败项按索引保留，已成功项移出队列，异常时不直接关闭弹窗。图片懒加载，不以网络预览失败阻止保存链接。
- URL 批量保存使用 addStickerUrlsToPack，一次写入分组索引，避免每张重复序列化整个库；既有单张添加入口不变。不抓取外部描述、不下载用户粘贴的图片，不修改其他页面设计。
- scripts/check-sticker-import.cjs 验证常见格式、签名链接、重复／无效链接、1000 条单次写入及文件部分失败；check-sticker-import-ui.cjs 在隔离真实 React 弹窗中验证重名、坏项保留、保存异常重试、预览加载失败与 500 条批量操作。实际手机验收仍用 3003。

- 2026-09-12 用户补充 dock 材质应与功能键一致：采用导航按钮同款日夜底色、22px 模糊／160% 饱和、细边及轻阴影（覆盖上一条临时 10px 模糊方案），左右各 20px 和 77px 高度保留。测试直接对比 dock 与功能键 computed style，避免各自漂移。

- 2026-09-12 dock 再调整：取消 340px 最大宽度，始终按应用屏幕宽度减 40px（左右各 20px）；77px 高度和上下留白不动。日间白底透明度 10%、夜间深底 16%，模糊从 30px 降至 10px，阴影减轻，移除灰色厚底，保留细玻璃高光边。

- 2026-09-12 dock 横向调整：用户认可高度、认为长度偏短；内页 dock 最大宽度由 276px 加至 340px，窄屏保留左右各 20px。77px 高度、上下 padding、底部位置和圆角不变，桌面 dock 不动。

### 2026-09-12 聊天页角色头像同步（未提交）

- 用户反馈列表和设置页已换头像、聊天页顶部仍旧图。ChatRoom 移除独立初始化角色 state，改 useChatCharacter/useSyncExternalStore 直接订阅角色存储事件和 focus，订阅时重新核对快照并随 contactId 更新，避免保留挂载时旧对象。
- scripts/check-chat-character-sync.cjs 用真实 React 与该 hook 在隔离浏览器中验证挂载状态连续两次更新、切换角色和返回后均显示最新 src；没有改用户头像数据或重放换头像动作。tsc 仅既有世界构建器缺依赖三项。

### 2026-09-12 系统日夜复查、缩小 Dock、MOMENTS 对齐（未提交）

- MOMENTS 从正文搬入 PageShell 标题槽，绝对居中在左右功能键之间，保持同一水平线；去掉原正文标题占位，资料卡自然上移。
- 依据用户 Dock.svg 的 368×103、38 圆角主体，约 75% 缩为 276×77、28.5 圆角，改轻磨砂底座，去掉粗白边和选中大胶囊，保留四页图标与标签；不修改桌面 dock。
- 补全聊天根节点夜间 header／panel／input-border／icon-active 等残留日间 token、原生控件 color-scheme；去掉消息和联系人添加好友页的硬编码白背景及头像底色。继续通过 prefers-color-scheme 实时跟随实际设备系统，没有新增手动开关。
- scripts/check-chat-system-theme.cjs 使用真实源样式层叠，在隔离页面验证四页日→夜→日无需刷新、背景与文字颜色、276×77 dock 和至少 44px 点击区域、MOMENTS 同线、320px 窄屏；截图为占位内容非真实应用截图。tsc 仅既有世界构建器三处缺依赖，3003 HTTP 200。实际手机观感待验收。

### 2026-09-12 续办子页、MOMENTS 与角色头像动作（未提交）

- 动态背景卡片上方新增居中 MOMENTS 小字标题。PhoneChatApp 给主页／动态所在内容层添加 data-social-pages，覆盖早返回的追发设置、离线推送、钱包、CSS、贴纸、插件、动态设置等 PageShell 子页：透明 header，保留必要标题及功能；发布弹层按钮统一玻璃感，不盲目删除评论／通知弹层的说明标题。
- 新增 lib/chat-avatar-action.ts：头像话题时向已开启识图且实际保留了图片的提示词发放候选消息 ID，仅允许当前聊天的用户图片／图片文件，排除表情、视频画面、撤回和缺失图片。视觉模型可选择不换，也可用普通对话邀请情侣头像；模型输出 [Avatar:消息ID] 才执行。
- 执行再次检查会话成员、候选来源、上下文、撤回及图片变化，十分钟有效且单次消费，拒绝外部 URL／伪造 ID。仅修改发言角色头像，发系统变更提示；私聊头像监听角色存储更新。私聊、群聊流式／非流式及本地后台追发均接入共享动作处理，非视觉／离线模式不发放候选。独立云端生成目前没有本机候选授权，不会执行头像修改。
- scripts/check-chat-avatar-action.cjs 校验普通图片、关闭识图、缺图、跨角色、撤回、伪造、重放与私聊／群聊成功路径。tsc 仅已有世界构建器缺 meshoptimizer / three-stdlib 三项；没有调用用户付费 API，真实模型理解图片／语境仍需手机测试。未提交／推送。

- 2026-09-12：按用户反馈，动态发布相机图标单独恢复 1.5 描边，避免统一导航 2.2 描边导致过粗；保留 27px 图标及 44px 按钮尺寸、位置和点击事件。

### 2026-09-12 主页背景顶部白块修复（未提交）

- 原因：资料区背景向上延伸，但 page-body 从导航下方开始且 overflow 裁切，顶部露出白底。
- 将主页滚动画布起点移到顶部，使用等量安全区＋导航高度 padding 保持头像、资料、钱包原位置；背景负向延伸量改成同一高度变量，覆盖返回键背后。返回键仍固定在原位置，渐隐终点仍为资料区底部。

### 2026-09-12 主页背景与聊天全局日夜（未提交）

- 按“全局日／夜”理解，沿用聊天室跟随系统模式，补齐消息、联系人、动态、主页通用颜色、菜单及底部导航暗色；没有增加单独主题开关或改动桌面主题。
- 主页资料区空白处可点击／键盘激活上传背景，使用现有 IndexedDB 图片资产存储，kv 键 chat_profile_cover_asset_v1；限制 20MB，最长边压到 1200px，读取／保存错误有提示，重复选同图可用。不发送图片到远端。
- 背景跟随资料区高度渐隐，到钱包上方透明，保留名字、ID 和功能区交互。刷新可加载，取消文件选择不变更现有背景。

### 2026-09-12 去掉边缘返回手势提示（未提交）

- 按用户反馈移除 EdgeSwipeBack 滑动时显示的 ChevronLeft SVG 和浮动底板；组件仅保留隐藏挂载点，不再每次滑动更新 React 提示状态。
- 左侧边缘向右滑动返回的方向、距离、速度阈值及调用原返回按钮的逻辑均未改变，原页面按钮不动。

### 2026-09-12 用户反馈修正：不加动态／主页标题，四页统一导航（未提交）

- 删除上一轮自行添加的两个正文大标题；主页资料区改为设置页式居中大头像（88px）、名字、真实 identity.id，剩余入口保留。
- 消息、联系人、动态、主页的根 header 使用同一套定位间距、44px 控件、27px SVG、2.2 描边及日夜玻璃边框；消息双功能键继续共用胶囊。仅根导航，不波及子页。
- 动态正文明确放在固定导航下方，避免内容跑到屏幕顶部。四页导航日夜隔离测试验证位置、尺寸、背景、边框、阴影完全一致；不代表手机视觉已验收。类型检查仍只有既有三处世界构建器缺依赖。

### 2026-09-12 动态与主页苹果风整理（未提交）

- 用户授权不等 ins 参考，直接按现有功能苹果化。仅两页添加 apple-social-page 作用域及 styles/chat-social-apple.css，未改底栏、聊天页、返回键几何尺寸。
- 动态正文大标题、紧凑封面个人资料卡、细分隔线内容流；保留封面上传／签名编辑／发布／点赞评论事件，移除原本写死的关注与粉丝等假数字。封面增加键盘访问。
- 主页大标题、资料卡、分组入口；移除顶部装饰渐变，钱包去除固定浅蓝背景和衬线金额，改为随日夜的中性卡片。现有入口及状态逻辑保留。
- scripts/check-chat-social-apple.cjs 静态事件钩子检查和 320/390px 日夜隔离布局测试通过，生成 qa/chat-social-*.png 并目视检查；并非真实用户数据交互验收。tsc 仅原有三处世界构建器缺依赖，diff --check 通过，3003 返回 200。未提交或推送。

### 2026-09-12 消息／联系人顶部留白（未提交）

- 消息与联系人标题搜索区 pt-5 + mt-2 改 pt-1，无额外标题上边距，统一上移约 24 CSS px。消息功能键位置、header 高度、安全区不动。
- 联系人去除 PageShell 的重复 Contacts 标题，保留正文“联系人”；只共享消息页的透明 header、玻璃按钮及搜索样式，不套用消息行布局。搜索与新的朋友入口中文化。
- New Friends 已有申请、接受、拒绝逻辑；修复无待处理申请时点击被条件拦截，现可打开原有“暂无好友申请”空态。未触发真实接受／拒绝请求。
- 动态及主页等待用户 ins 参考，未改。diff 检查通过，tsc 仅既有三处世界构建器依赖错误；浏览器连接超时，手机视觉待 3003 验收。

### 2026-09-12 隐藏小组件通用英文标签（未提交）

- 按用户要求移除 WidgetRenderer 共用的 widgets 底部字样（覆盖内置及 DIY 桌面小组件），以及查手机页面音乐组件的 Widgets 标签。仅移除这两处通用标签，不修改用户 HTML 内容、组件功能、网格定位或 App 名称。

### 2026-09-12 仅优化音乐浮窗（未提交）

- 用户明确只改 MusicFloat，不改桌面组件或其他音乐页面布局，不做灵动岛。
- 增加音乐设置“悬浮播放器”持久开关（kv 键 ai_phone_music_float_enabled_v1）；浮窗 × 关闭该开关，播放／换歌不会强行重新开启。原左甩隐藏移除暂停操作，临时隐藏与长期关闭均不影响音频。
- 浮窗收起 60px、展开 310×76px（窄屏限宽），中性日夜玻璃配色；展开显示完整封面、复用长歌名滚动，保留播放和切歌。ResizeObserver 限制展开后越界，pointercancel 不再误触收起／打开。
- 静态行为断言及 git diff --check 通过；tsc 仅既有世界构建器 meshoptimizer / three-stdlib 三处缺依赖，未新增错误。手机最终视觉待 3003 验收，未推送。

### 2026-09-12 独立桌面音乐组件第一版（未提交）

- 手机反馈后改为通透玻璃日夜款，去掉新两款组件底下“音乐”标签；封面主色仅轻度染色。每个组件的日月按钮循环跟随系统／日间／夜间，保存到该组件 config.glassTheme，不改变 App 或其他组件主题。
- 新增 MusicMarquee：ResizeObserver 检测溢出，长歌名两端停顿往返滚动，换歌重启；减少动态效果时静态省略并保留完整 title／按钮读屏名称。
- 音乐底栏裂图来自 public/birds 中不存在的四张装饰小鸟，已移除对应失效图片引用；不删除用户图片。P4 确认为 MusicFloat 的原有展开形态，本轮未重设计或移除，避免与桌面组件同时改动。
- 回归补充主题配置保存、系统暗色切换、滚动动画与减少动态效果，以及失效 birds 引用检查。日夜隔离截图已检查；3003 可访问，手机最终视觉待验收。

- 用户明确纠正：保留音乐 App 原入口，桌面组件独立添加，不替换应用图标；保留原悬浮唱片和聊天音乐卡片。
- 新增「音乐 · 小方块」2x2 与「音乐 · 横向卡」2x4，沿用外观／桌面组件添加流程。横向卡下排是真实播放队列前四首（去重），不是虚构推荐或云端歌单；不足四首不填假数据。
- 共用 MusicControls：播放／暂停、点队列选歌、点标题打开播放器；无当前曲目时打开音乐 App，组件库预览禁用交互。没有新增 Audio 引擎或修改音源配置。
- 保留原封面，24x24 采样主色后等比压暗作底，白字；64 项内存缓存、切换／卸载防旧取色覆盖，缺图／跨域失败回退中性底色。新增独立样式，不改旧组件外观。
- check-live-music-widget.cjs 验证共享控制、预览不播放、无歌曲入口、队列去重、红蓝封面取色和 148x160／320x160 排版；隔离截图 qa/live-music-widgets.png 已检查。tsc 只有原有 world-builder 三项依赖缺失。3003 HTTP 200；尚待真实手机添加与播放验收。
- 尚未实现黑胶款、横向全控制款、歌词展开页、聊天卡片美化或独立分发包；本版是宿主原生内置组件，不宣称任意 HTML 组件包可控制其他播放器。

- 后续电脑内置浏览器与手机验收统一使用 http://192.168.101.2:3003/，不要自行切回 127.0.0.1:3000 或换端口。
- 2026-09-12 用户确认已经自行完成备份导入，不要重复迁移、覆盖或清空现有预览数据。
- 3003 是同一份源码的局域网入口，桥接至 127.0.0.1:3000 开发服务；修改后刷新即可验收，不需要先提交或 push。验收满意后，仍须用户明确要求才提交／推送。
- 电脑须开机、开发服务与桥接服务均须运行，手机须处于可互通的同一局域网。桥接脚本：C:/Users/Effy/Documents/Codex/2026-08-23/link-dom-css-x20/outputs/keyboard-ab/float-lan.cjs。仅用于可信局域网；IP 改变时先说明，不要默默换入口。
- 不同地址的数据隔离；不同设备即使使用同一地址，本地数据也不自动同步。保留旧地址数据，不再把旧地址作为默认验收页面。
- 用户接受目前键盘辅助栏背景的取色融合效果，先保留、不再继续调整；这是近似底色融合，不是移除系统背景或控制其透明度。

## 首要目标与原则

### 2026-09-12 iOS 键盘背景真机对照（未提交）

- 后续真机截图仍有深蓝矩形底色：未验收通过。发现浏览器底色误用状态栏顶部 40% 取色，现拆分为顶部／下部采样，测试断言深顶浅底图片两者分别输出。图片下部平均色仍是近似，非壁纸连续绘制；原生辅助栏未确认可控，不得宣称已消除。

- 手机 A/B 显示 enterKeyHint 影响回车键样式，但两版辅助栏背后均无原先灰块，因此不再把它当作灰块根因；保留聊天输入与发送逻辑。
- 发现白色聊天内部与 html/body/app-root 的灰色背景不一致。复用现有页面背景采样，用独立 --mobile-browser-canvas 同步手机底层画布，不覆盖主题 token，不改变页面高度。聚焦输入及聊天暗色切换时重算；图片使用已有平均色近似，不承诺原生辅助栏可被 CSS 控制。
- 新增 check-mobile-browser-canvas.cjs：白色、暗色、彩色、退出页面恢复、主题隔离通过；tsc 仍仅有原有三项依赖缺失错误。
- 完整 float 局域网入口 http://192.168.101.2:3003/，临时桥接到 127.0.0.1:3000，HTTP 200 已确认。桥接脚本在当前任务 outputs/keyboard-ab/float-lan.cjs，不进入产品。新 origin 数据独立；同局域网可访问，勿用于不可信网络；原生键盘效果待手机验收。未提交或推送。

- 按 Effy 的喜好修改 float 源码，形成持续维护的二改版本。优先服务自己的完整使用体验，不为可分发性牺牲核心功能。
- 保留功能，视觉尽量向 iMessage / iOS 的克制、清透、细致靠拢，而非逐像素复制。
- 适合独立使用的 CSS / 插件，后续整理为社区分享资源；完整源码改动和可安装资源包是两条不同的分发线。
- 尊重原项目开源许可与署名；发布前核对许可、素材授权、版本兼容和隐私。开源不代表任意素材都可再分发。
- 除明确要求，不提交、不推送、不上传。测试不发送真实 AI 消息、不改聊天数据。
- 预览固定使用上述 3003 局域网入口；127.0.0.1:3000 仅为底层开发服务及旧数据入口。不要为启动方便更换端口。

## 已实现 / 已迭代的二改成果

以下基于本对话实施记录；“已实现”不等于手机、暗色、所有消息类型都已完整验收。

### 私聊、群聊共用外观与交互

- iMessage 风格气泡、尾巴、玻璃输入框与功能菜单；输入框圆角调整并保留主题 / 信息两栏。
- 聚焦输入或有文字时显示发送按钮，否则显示语音入口；独立发送与触发 AI 回复分工。保留回车发送设置，后续持续回归。
- 移除 emoji 文字联想候选；贴图候选独立显示、与输入区域对齐、限制尺寸并等比适配。
- 引用手势降低触发门槛：横向锁定约 5px，触发约 8px，容忍一定斜滑；菜单打开后禁用引用。手机手感需继续验收。
- 引用排版取消不必要缩进；思考入口放入引用结构内；引用连接线端点视觉整理。
- 系统消息菜单使用统一玻璃视觉；翻译保留原文，配分隔与小字号译文，可展开 / 收起。
- 地图卡改用 SVG pin、示意路线及图片右下角 Maps 标识，缩小卡片；转账与地图尺寸统一方向已落实。红包压尾巴的间距问题曾修复，需媒体混排回归。
- 主功能菜单去除重复视频通话、语音通话、语音条入口；改为直接长按拖动排序，无箭头或排序说明；松手后保存顺序，拖动使用逐帧位移。
- 私聊 / 群聊连续气泡间距统一为紧凑方向；两者字号与内边距仍有差异，不应称为完全相同尺寸。

### 群聊辨识与 Tapback

- char 头像绑定一组回复末条消息底部；名字位于首条上方、对齐文字起点；备注优先使用私聊备注，再回退角色名。
- 群成员切换的组间距与同人连续气泡间距分开处理；群聊更紧凑。
- 群头像支持自定义（本地 / URL），默认保留组合头像；设置入口统一字号。
- 从头像本地取色，用于群成员名字；头像更新时重新取色，无彩色或提取失败时回退灰色。
- Tapback 不应强制每条回复都出现；支持 emoji 动作解析和按参与者记录群聊反应，堆叠概览与展开详情。
- 私聊更换反应需真实更新数据而非只在台词中宣称；模型执行效果仍应实测。
- 群聊模型动作当前不应宣称支持任意历史消息定位；本轮没有扩大历史上下文发送范围。

### 日夜、背景与未读

- 聊天页跟随系统日夜模式；壁纸本地亮度采样，用于深色壁纸下的引用、名字等对比度适配。跨域取色失败不上传或代理图片。
- 移除整片 header / bottom 遮罩；气泡自身保留边缘淡化，文字头像不随整片蒙版消失。
- 优化输入提示、引用气泡 / 连接线、群成员名字、时间等暗背景显色。
- 转账与礼物卡补充夜间表面、文字、状态、分隔线，转账详情同步处理。其他页面 / 卡片尚未全部适配。
- 新增未读计数、已读处理；返回按钮内含未读数字胶囊，和箭头居中对齐；消息首页蓝点与未读过滤读取未读状态。
- 夜间迭代明细另见 chat-night-mode.md；每个后续定稿页面都需补日夜适配。

### 消息首页

- 参考列表行：圆头像、名字和时间同排、两行预览、细分隔线；保留名字旁装饰性认证勾（不代表真实安全认证），不显示在线绿点。
- 右上角新建 / 过滤合并玻璃胶囊，使用用户提供的 SVG；过滤含全部、私聊、群聊、未读；新建保留原流程。
- 搜索保留原位置，后改为平面浅灰底，无悬浮阴影。移除顶部用户头像和名字。
- 底部 dock 轻透玻璃、黑灰图标；蓝色主要留给未读提示。功能子菜单统一视觉。
- 修复首页 header 三列布局造成的右侧按钮偏中；修复首页 header 覆盖私聊 / 群聊的层叠问题。
- 减弱按钮外阴影；另外去掉整条 header 继承的 backdrop-filter，消除屏幕边缘黑灰晕染。按钮自己的玻璃保留。

## 历史实验与回退点（已由下方会话设置替代）

- 已做群聊 char 普通文字气泡 10% 头像色 + 原灰底实验；流式预览同步；名字不变，卡片和 user / 私聊不染色。
- 当时入口为 components/chat/use-group-bubble-tint.ts 的 GROUP_BUBBLE_TINT_PERCENT。现已替换为会话配置，不再使用此常量；关闭改色请使用「恢复经典蓝／灰」。
- 实验前快照在本机 `C:/Users/Effy/Documents/Codex/2026-08-23/link-dom-css-x20/checkpoints/before-group-bubble-tint-20260911/`，含 chat-room.tsx、imessage26.css 和说明。
- 快照包含最初 header 层叠修复，但不含之后移除整条 header 模糊的改动。回退时只撤销实验相关差异，不整文件覆盖。
- 2026-09-11 最新反馈：灰底叠色偏灰，倾向浅色模式从白底叠加低比例头像色。此新方案尚未实现。

## 会话级「气泡改色」原始需求

用户意图（历史记录；最新实现见下）：

- 设置仅影响当前聊天窗口，不修改角色头像或其他窗口颜色。
- 私聊：user 手动颜色；char 支持自动头像取色 / 手动选色。
- 群聊：user 手动颜色；char 同样支持自动 / 手动。手动是全群统一色还是按成员设置，尚待确定。
- 浅色自动配色以白底为基础叠少量头像色，不还原原始色、不做高饱和色块。
- 建议保留经典蓝 / 灰复位选项；夜间需独立使用深底淡染，不能直接套白底。手动颜色需处理文字对比度。
- 名字沿用已有取色；尾巴跟随气泡；地图、转账、礼物等有自身样式的卡片默认不染色。
- 推荐新增配置默认保持现有经典模式，实验不直接变成全局默认；待用户确认。

## 2026-09-12：会话改色与语音气泡

- 聊天设置新增「气泡改色」，仅保存当前会话：user 手动选色；char 经典 / 自动头像取色 / 手动。群聊手动暂为全群统一 char 色，自动则分别采样各自头像。
- 默认经典蓝 / 灰，可复位；自动日间白底混入 12% 头像色、夜间深底混入 24%；头像变化后重新取色，失败或无彩色回退经典。地图、转账等独立卡片不染色。
- 气泡正文对比度随颜色调整；语音用户默认白色图形、经典 char 使用蓝色系图形；自定义浅色表面使用深色图形兜底，避免白色消失。
- 私聊 / 群聊语音采用平面播放键、细波形，同一内容容器内展开文字，与语音左右等宽；思考入口移至此容器左侧。保留菜单转文字，并新增直接展开 / 收起入口。
- 保留现有音频播放和按需 TTS，加入真实暂停 / 继续以及精简失败提示；没有新增语音识别服务，转文字仍读取消息已有文字。
- 验证：scripts/check-bubble-colors.cjs 配色回归通过，git diff --check 通过；类型检查仍只有现有 meshoptimizer / three-stdlib 三项缺依赖报错。不触发收费 TTS。原地址 127.0.0.1:3000 已恢复，但浏览器仍显示应用加载中，未完成聊天内视觉验收；真机播放、日夜和长文本混排待测。

## 其他遗留与未来构想

### 2026-09-12：手动配色修复、应用范围和记录

- 手动模式不再依据气泡明暗改文字颜色；黑字黑底、同色文字均按用户选择保留。对比度平衡仅用于 char 自动头像配色。
- 编辑使用同步草稿引用合并每个颜色 / 透明度字段；配置迁移为独立 KV 数据，不再随普通聊天会话写回而被旧快照覆盖。没有新配置时仍读取旧会话的 bubbleColors。
- 气泡底色与尾巴先作为不透明背景层绘制，再整层应用透明度，文字独立绘制；覆盖预览、普通消息、引用回复和流式气泡，避免接缝处重复叠色。
- 移除色谱和 RGB 滑杆入口，保留网格 / HEX / 不透明度，新增「应用范围」页面：当前聊天 / 全部聊天，显式点击应用。
- 使用递增版本确定覆盖顺序：最新全局覆盖所有旧设置，也影响未来新会话；之后的新本地配置仅覆盖该会话；再应用全局会再次统一。
- 关闭已编辑的面板或点击应用时记录完整方案，支持载入历史方案到当前聊天，再自行决定是否全局应用。不记录每个拖动帧；不把切换历史直接视为全局覆盖授权。
- 回归检查通过：手动黑字黑底、透明度保留颜色、全局 / 本地优先级、新群继承、历史载入、持久化读写。类型检查仍只有已有三个 world-builder 缺依赖错误。预览服务因 4GB 堆内存耗尽退出，已原地址重启；浏览器停在连接错误页，最新视觉需重新加载验收。

### 2026-09-12 参考稿修订：设置页与配色详情

- 根据用户提供 List.svg：聊天设置去除磨砂 header，仅保留返回圆胶囊；居中 char / 群头像，下方直接显示可点改备注 / 群名。更换群头像时顶部同步更新。
- 私聊、群聊共享 iOS 分组列表：功能设置、AI 设置、外观与体验、群聊管理、会话管理。保留原有条件与事件处理；不删除电脑入口、图片上下文、自动发送等功能。删除类动作仍走原确认流程。
- 新增独立的配色详情页，保持在手机容器内，不展开挤占列表。参考 Color Picker.svg 实现网格、色谱、RGB 滑杆、HEX 输入、棋盘格不透明度与常用色。
- 我 / 对方（群成员）分别配置气泡颜色、不透明度、文字颜色、不透明度；自动模式开关启用后手动区域禁用变灰，手动值保留。自动取色仍按每位成员头像计算，不复制预览首位成员颜色到全群。
- 配色统一解析为背景 RGBA 与文字 RGBA，不使用整气泡 opacity，因此文字和背景可独立调整；引用回复正文也跟随当前窗口配色，独立卡片不染色。
- 语音从原 SVG 直接提取圆形播放键路径、40 根波形坐标 / 高度，保留 280×76 比例及时间基线，动态显示分:秒；底色使用平面 iOS 气泡，无参考中的立体渐变。窄屏按比例缩小。保留播放、暂停 / 继续及转文字。
- 验证：配色与独立透明度回归、自动覆盖、回退和波形几何检查通过。已看到真实浏览器的分组列表与手机容器内选色页；未触发付费 TTS，群聊及夜间仍需真机验收。类型检查仍只有原有三个 world-builder 缺依赖错误。

### 明天待办（2026-09-12，用户指定；仅记录，未启动开发或创建提醒）

1. 自动改色功能：推进会话级气泡配色；浅色白底淡染、日夜区分，user 手动，char 自动 / 手动 / 经典；群聊手动色作用范围待确认。
2. 语音气泡功能：此前聊天美化遗漏的消息类型，检查现有播放与转写能力，补齐私聊 / 群聊外观、播放状态和日夜模式。不默认包含新增 TTS 或语音识别服务。
3. 整个聊天页面的更新：作为整体验收与补齐项目，覆盖列表、私聊、群聊、设置 / 子菜单、各类卡片与日夜；新增页面重构范围需进一步确认。
4. char 自动头像：聊天 app 内头像覆盖层，不覆盖原角色卡；头像来源、触发条件、用户允许范围待设计；私聊 / 群聊 / 动态同步并重新取色。
5. 音乐卡片：结合现有音乐功能评估可转发和聊天内真实播放；验证音源、跨域、鉴权、浏览器播放限制，不承诺任意平台链接都可播放。
6. 真实小组件：替代当前默认 1:1 app 图标卡片，提供多种入口尺寸 / 布局；可展示真实 char 聊天摘要或个人信息，点按进入对应页面。初步理解为 float 内桌面小组件；若指 iOS / Android 系统桌面，需另行评估原生能力。须考虑数据更新、隐私遮挡和点击路由。

建议顺序：配色与语音 → 聊天页整体回归 → 自动头像 → 音乐卡片 → 小组件。不是完成时限承诺。

- 手机引用灵敏度、纵向滚动冲突、长按菜单和拖动排序继续真机验收。
- char 自主换头像：仅聊天 app 内（私聊 / 群聊 / 相关动态）生效，尚属构想。
- 可转发、聊天内能实际播放的音乐卡片：需衔接现有音乐功能，尚未实施。
- 夜间逐页推进，不把“聊天页首轮”称作全局夜间完成。
- 完整版本与原版资源包分发分离；资源库支持 CSS / JS 插件，但新增 React 结构、存储、手势和 AI 管线不能直接靠 CSS 搬运。
- 插件无沙箱，与宿主同权限。分享插件需披露数据访问与网络行为，优先本地处理、可禁用、可卸载、明确兼容版本。

## 工程状态与验证限制

### 2026-09-12 引用语音与来源配色修复（18f655c 后）

### 2026-09-12 群聊 Tapback 与夜间菜单复查（a109461 后，未提交）

- 菜单拖动收尾修复：原先先清 transform 再提交排序会产生跳动；现记录松手时每行的实际位置，在 useLayoutEffect 中对新布局执行 220ms FLIP 收尾，保留滚动位置并禁用自动滚动锚定，支持缩放、取消和减少动态效果。拖动交互与私聊／群聊排序存储不变。
- 夜间细节补记：返回键未读数字不应沿用日间的镂空字；夜间改为无 mask 的实心白字／深灰底，日间原样保留。主菜单、上下文子菜单与输入框高光接入夜间玻璃阴影变量，避免日间亮白高光。check-menu-reorder 与日夜菜单测试新增未读实际组件显示、文字填充和无遮罩验证。
- 新增短气泡 Tap 截断：叠层可视宽度不超过正文气泡宽度，仅内部视觉层裁剪，点击区域和全部成员数据保留。
- 新增手机屏幕左缘右滑返回（共享 phone-screen 接入）：左侧 24px 起滑，10px 意图判断，横向位移需大于纵向 1.6 倍；普通滑动阈值为屏宽 24%（72–120px），快划至少 48px / 300ms 内 / 0.5px/ms；松手才提交，超过 2.5 秒取消。显示轻量箭头进度提示，不移动整页或改原布局。只调用当前可见、未被遮挡的返回按钮，保留子页层级与既有保存确认；无返回按钮不操作。纵向、中部、多指、touchcancel、不可取消事件、选中文字和输入／画布等区域不触发。不调用 history.back，不改变浏览器历史。
- 手势参考 MDN Touch Events 的方向／距离／时间判断和取消处理：https://developer.mozilla.org/en-US/docs/Web/API/Touch_events/Using_Touch_Events 。测试覆盖阈值、子页、遮挡、取消和清理；iOS Chrome 的浏览器边缘手势优先级仍需手机实测，不宣称已做真机验证。
- 截图补充回归覆盖 `[Oneone]: [Tapback:😘]` 单独角色段，与另一角色同条消息回应共存。
- 按最新验收将 22×26px 放大 1.3 倍至 28.6×33.8px，emoji 15.6px，恢复贴住正文气泡角的位置（不再悬在上方）。群聊单人和私聊使用同一尺寸、锚点与带尾巴 SVG；多人首个表情完整置顶，后方圆形表情每层错开 8px，仅露出一部分，不横向平铺。点击仍展示全部成员与回应，保留计数和至少 44px 高的透明点击区域。回归验证单人几何完全一致、多人前后遮挡顺序与贴边位置。
- 实际预览排查发现页面仍留着 33px、没有 SVG 的旧圆片，而源码已更新；当时 3000 端口无监听，已从本仓库恢复预览服务。用户正在生成回复时不强制刷新，避免中断。

- 用户反馈 iOS 26 / iPhone 17 Pro Max / Chrome 下，编辑回复中有 Tap 标记但气泡不显示（不是运行时报错）。查出群聊工具轮 onTextPart 路径未执行 Tap、只保存动作消息；现与最终回复一样按角色执行并跳过动作气泡，多人互不覆盖。
- 显示正则的消息重新解析原先会覆盖 mediaData、丢失持久化反应，现保留 tapback / tapbacks。未重放旧回复动作或修改历史消息。
- 多人反应读取／展示增加异常数据过滤和按 actor 去重；表情验证不再依赖 Intl.Segmenter，保留任意单个 Unicode emoji 及组合序列，不更改角色表情多样性提示。
- 夜间长按主／子菜单移除写死的深色文字与日间分隔线；设置子页 CSS／搜索／Tapback 候选去除写死白底。设置控件和确认框接入同一夜间变量，保持无磨砂 header。
- 验证：check-imessage-interactions 执行真实群聊分段保存循环与显示投影回归；check-chat-night-menus 使用独立无用户数据的浏览器检查日夜菜单计算样式、四人反应堆叠和设置子页。类型检查仍仅既存三项 world-builder 缺失依赖。手机需后续部署测试。

### 引用与语音修复明细

- 纠正引用样式作用范围：只有已发送引用摘要使用透明描边；输入框上方待发送引用恢复原有实心气泡预览。预览正文／尾巴合成一个表面后统一应用来源颜色和透明度，避免异色尾巴；非文字摘要与恢复默认联动保留。新增两者样式隔离回归断言。

- 新增 ElevenLabs 语音供应商：设置／配置持久化、账户音色分页同步、手填 Voice ID、模型与声音参数、共用试听／聊天语音／通话 TTS。不同供应商切换清理旧密钥音色；不改变 STT 和已有 MiniMax／OpenAI 合成。
- 接入细节见 `docs/elevenlabs-voice.md`；模拟测试 `scripts/check-elevenlabs.cjs` 不消耗额度，真实账户试听待用户配置。尚未提交或推送。

- 描边复查：引用日间默认仍为灰色（约 #b5b5b9），不改。设置及子页面返回／同类圆形功能按钮改用独立白色玻璃描边变量（日间白色 90%，夜间 22%），对齐聊天按钮；不改变列表分隔线或添加 header 磨砂。

- 引用保存不再只读取 content：语音从正文／label 生成摘要，无文字显示“语音消息”，避免空摘要只剩连接线。
- 已存空摘要在显示时尝试通过 quoteMessageId 找回来源，来源不在已加载消息内时显示占位，不重写历史数据。
- 引用恢复透明底细描边，输入框预览与已发送引用统一；去除预览实心尾巴、投影及磨砂。描边实时读取来源人物的当前气泡色，恢复默认后不保留自动色；文字按日夜／深背景保证可读性，回复正文保持原配色。
- 语音完整复制可用文字，转账／红包显示金额和备注，礼物显示名称和留言，其他卡片提供带类型的可读摘要；原消息已加载时也修复旧的空白／截断引用，不修改历史存储。
- 回归覆盖带旧头像采样时从自动色恢复默认、完整语音、转账（含零金额）、礼物及卡片摘要；浏览器确认已有文字／表情引用显示透明描边、默认中性边框，不再残留蓝色填充。手机实际效果待用户测试。
- 本次仅修复并验证，尚未追加提交／推送；此前提交为 18f655c。

### 2026-09-12 群聊分泡排查／转账去阴影

- 转账卡片去掉外阴影及内高光阴影，私聊／群聊和日夜模式共用；边框、尺寸、交互不变。
- 群聊生成按角色段解析，随后共用 rich-message-parser 按双换行分泡；流式预览同样按双换行。当前差异未修改该拆分规则，群聊引擎差异只有 Tapback 提示注入。
- 单换行仍是同泡内换行；显示正则会先处理原文再重解析，因此压缩空行的用户正则也可能影响呈现。截图具体原因仍需该轮原始回复／实际正则确认，未擅自重拆历史消息或修改提示词。
- 增加真实流式拆分函数回归：同样三句话单换行为 1 条，双换行为 3 条。

### 2026-09-12 子页面统一与头像色彩个性

- 双语提示词、全屏特效由跨手机弹窗改为手机内完整设置子页；保留编辑、恢复默认、保存及特效预览能力，私聊和群聊共用。
- 所有嵌套设置 PageShell 的导航改为正常流布局，取消固定遮挡；气泡改色导航和内容作为同一个滚动面，不再裁切气泡预览。必要确认弹窗约束在手机内。
- 群头像取消管理列表重复行，点击设置顶部头像展开本地／URL 选择和恢复默认。
- 自动取色不再统一头像主色的明暗、饱和度；白底混色强度随头像变化，夜间保留主色明暗层次，灰阶头像也保留明暗区别。语音控件仍保证对比度，手动颜色不受影响。
- 回归新增同属蓝色的深海蓝、冰蓝、灰蓝输出差异断言，避免只针对某张头像调色。

### 2026-09-12 配色与语音二次打磨

- 配色历史改为左滑、右侧删除；移除含义不清的箭头。名称可原位修改（最多 40 字），色块／日期载入；名称和删除都不影响现用配色。
- 自动气泡提取头像主色，不再复用压暗后的名字颜色；日间从白底叠加标准化主色，夜间保留同色相深色。灰阶／取色失败走中性回退，缓存区分名字与气泡，头像更新后丢弃旧样本。语音控件用同色系高对比色，手动配置不做自动平衡。
- 发送按钮背景／箭头跟随 user 气泡／文字及透明度；语音展开文字增加 7px 间隔，菜单随状态切换“转文字／收起文字”。
- 设置页私聊／群聊头像统一 62px、备注字号与聊天顶部一致，并保持头像与返回键居中对齐。
- 自动色在十组极端色及灰阶下验证日夜文字／语音对比度 >= 4.5；保存名称和删除记录均通过不影响现用方案的存储测试。

### 2026-09-12 设置细化与配色历史

- 私聊／群聊设置：头像与返回键中心对齐；备注缩为 19px，去掉箭头，原位输入，失焦／回车保存，Escape 取消，兼容中文输入法。
- 自定义状态栏改为手机内子页面，保留契约、HTML、导入导出、方案与预览；聊天设置内所有 PageShell 导航取消底色、磨砂和阴影。CSS 入口置于外观与体验最后。
- 语音气泡统一收窄至 220px（小屏按比例限制），移除外置转文字按钮；菜单转文字和同宽文字展开保留。
- 配色历史支持右滑露出删除按钮、点击删除；删除只移除记录，不改当前／全局配色与覆盖顺序。键盘右箭头也可展开，Escape 收回。
- 存储回归增加删除不影响现用配色断言；类型检查仍仅有原有三项缺失依赖错误。

- 主仓库 `C:/Users/Effy/Documents/Codex/ai-virtual-phone`，分支 feat/imessage-private-chat；本轮大量改动仍未提交、未推送，以 git status 为准。
- middleware.ts、Supabase weixin-assistant、tsconfig.json、qa/ 有其他改动，整理提交时不能盲目一起加入。
- 近期 tsc 检查只有已有 meshoptimizer / three-stdlib 缺失造成的三个错误；不能声称项目完整类型检查通过。
- 页面曾因本地服务 / HMR 中断显示旧内容或裂图；诊断时区分服务状态、旧数据图片和代码错误，不能清除用户数据来解决。
